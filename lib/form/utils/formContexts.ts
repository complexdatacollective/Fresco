// Legacy form contexts - this file maintains compatibility with the old form system
// TODO: These will be replaced by the new form system implementation

export const formContexts = {
  // Legacy form context exports for backwards compatibility
  // This prevents build errors while migrating to the new form system
};

// Legacy hook for backwards compatibility
export const useFieldContext = () => {
  // Return empty object for now to prevent build errors
  // This will be replaced with actual implementation
  return {};
};
