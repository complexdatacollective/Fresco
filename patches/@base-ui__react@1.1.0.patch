diff --git a/esm/composite/list/CompositeList.js b/esm/composite/list/CompositeList.js
index 8277bc59c5052f28c179720d9498f8a5853d88a2..0b1319302c4e834eb6ca72ff1d82bb4d4f152907 100644
--- a/esm/composite/list/CompositeList.js
+++ b/esm/composite/list/CompositeList.js
@@ -100,18 +100,10 @@ export function CompositeList(props) {
     }
     onMapChange(sortedMap);
   }, [onMapChange, sortedMap, elementsRef, labelsRef, mapTick]);
-  useIsoLayoutEffect(() => {
-    return () => {
-      elementsRef.current = [];
-    };
-  }, [elementsRef]);
-  useIsoLayoutEffect(() => {
-    return () => {
-      if (labelsRef) {
-        labelsRef.current = [];
-      }
-    };
-  }, [labelsRef]);
+  // REMOVED: Cleanup effects that break React 18 Strict Mode.
+  // In Strict Mode, effects are double-invoked (setup -> cleanup -> setup).
+  // The cleanup clears the array, but ref callbacks don't re-fire to repopulate it.
+  // See: https://github.com/mui/base-ui/issues/XXXX
   const subscribeMapChange = useStableCallback(fn => {
     listeners.add(fn);
     return () => {
