# Docker Compose Environment Variables for Test Stack
# Copy this file to .env.test and customize for your instance

# For running multiple instances, use different values for each instance
# Example for instance 1: APP_PORT=3000, POSTGRES_PORT=5433
# Example for instance 2: APP_PORT=3001, POSTGRES_PORT=5434
# Example for instance 3: APP_PORT=3002, POSTGRES_PORT=5435

# Application port (default: 3000)
APP_PORT=3000

# PostgreSQL port (default: 5433)
POSTGRES_PORT=5433

# Optional: Set a unique project name for each instance
# This will prefix all container names, networks, and volumes
# Example: COMPOSE_PROJECT_NAME=fresco-test-1
# COMPOSE_PROJECT_NAME=fresco-test